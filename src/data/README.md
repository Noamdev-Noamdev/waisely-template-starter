/**
 * Sessiegegevens worden NIET in dit bestand opgeslagen — zie src/lib/auth.ts.
 * Dit bestand bevat de gebruikersaccounts uit de JSON-store (seed.ts genereert
 * de admin- en demo-accounts bij eerste start).
 * Plaats deze bestanden in src/data/; ze worden automatisch aangemaakt.
 */
